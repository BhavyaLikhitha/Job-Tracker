{
  "name": "job-tracker",
  "private": true,
  "type": "module",
  "description": "Root test runner for the Job Tracker monorepo (client + server).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "mongodb-memory-server": "^10.4.3",
    "supertest": "^7.2.2",
    "vitest": "^4.1.8"
  }
}
